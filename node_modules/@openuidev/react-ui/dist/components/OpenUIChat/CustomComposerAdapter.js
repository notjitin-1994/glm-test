import { jsx as _jsx } from "react/jsx-runtime";
import { useThread } from "@openuidev/react-headless";
export const CustomComposerAdapter = ({ composer: Composer }) => {
    const processMessage = useThread((s) => s.processMessage);
    const cancelMessage = useThread((s) => s.cancelMessage);
    const isRunning = useThread((s) => s.isRunning);
    const isLoadingMessages = useThread((s) => s.isLoadingMessages);
    return (_jsx(Composer, { onSend: (message) => {
            processMessage({ role: "user", content: message });
        }, onCancel: cancelMessage, isRunning: isRunning, isLoadingMessages: isLoadingMessages }));
};
//# sourceMappingURL=CustomComposerAdapter.js.map