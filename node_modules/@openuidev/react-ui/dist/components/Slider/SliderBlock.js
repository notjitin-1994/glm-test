import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import clsx from "clsx";
import debounce from "lodash-es/debounce";
import { AlertCircle } from "lucide-react";
import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import { Input } from "../Input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "../Select";
import { Slider } from "./Slider";
const ValueInput = ({ value, onChange, error, disabled, }) => {
    const [inputValue, setInputValue] = useState(value);
    const [isFocused, setIsFocused] = useState(false);
    useEffect(() => {
        if (!isFocused) {
            setInputValue(value);
        }
    }, [value, isFocused]);
    return (_jsx("div", { className: "openui-slider-block__validated-input", children: _jsx(Input, { type: "text", value: inputValue, onChange: (e) => {
                const { value: newValue } = e.target;
                setInputValue(newValue);
                if (!isNaN(Number(newValue))) {
                    onChange(Number(newValue));
                }
            }, className: clsx("openui-slider-block__input", {
                "openui-slider-block__input-error": error,
            }), disabled: disabled, onFocus: () => setIsFocused(true), onBlur: () => setIsFocused(false) }) }));
};
export const SliderBlock = (props) => {
    const { label, name, variant, min = 0, max = 100, step, defaultValue, disabled, ...sliderProps } = props;
    const [value, setValue] = useState(defaultValue ?? [min]);
    const { min: minError, max: maxError } = useMemo(() => {
        const [minValue, maxValue] = value;
        const checkValue = (v) => {
            if (isNaN(v))
                return "Invalid number";
            if (v < min || v > max)
                return `Value must be between ${min} and ${max}`;
            return "";
        };
        const error = { min: checkValue(minValue), max: checkValue(maxValue) };
        if (value.length > 1 && minValue > maxValue) {
            error.min = "Min must be less than max";
        }
        return error;
    }, [value, min, max]);
    const onValueCommitRef = useRef(sliderProps?.onValueCommit);
    onValueCommitRef.current = sliderProps.onValueCommit;
    const debouncedOnValueCommit = useMemo(() => debounce((newValue) => {
        onValueCommitRef.current?.(newValue);
    }, 200), []);
    useEffect(() => {
        return () => {
            debouncedOnValueCommit.flush();
        };
    }, [debouncedOnValueCommit]);
    const setValueAndCommit = useCallback((newValue) => {
        setValue(newValue);
        debouncedOnValueCommit(newValue);
    }, [debouncedOnValueCommit]);
    useEffect(() => {
        setValue(defaultValue ?? [min]);
    }, [defaultValue, min]);
    const isRange = value.length > 1;
    const isDiscrete = variant === "discrete";
    const effectiveStep = isDiscrete ? (step ?? 1) : Math.max(1, step ?? 1);
    const controlElements = useMemo(() => {
        if (isDiscrete) {
            const allOptions = Array.from({ length: Math.floor((max - min) / effectiveStep) + 1 }, (_, i) => min + i * effectiveStep);
            return isRange ? (_jsx("div", { className: "openui-slider-block__controls is-range", children: _jsxs("div", { className: "openui-slider-block__validated-select-container", children: [_jsxs(Select, { value: String(value[0]), disabled: disabled, onValueChange: (val) => setValueAndCommit([Number(val), value[1] ?? max].sort((a, b) => a - b)), children: [_jsx(SelectTrigger, { children: _jsx(SelectValue, {}) }), _jsx(SelectContent, { children: allOptions
                                        .filter((o) => o < (value[1] ?? max))
                                        .map((o) => (_jsx(SelectItem, { value: String(o), children: String(o) }, o))) })] }), _jsx("div", { className: "openui-slider-block__separator" }), _jsxs(Select, { value: String(value[1]), disabled: disabled, onValueChange: (val) => setValueAndCommit([value[0] ?? min, Number(val)].sort((a, b) => a - b)), children: [_jsx(SelectTrigger, { children: _jsx(SelectValue, {}) }), _jsx(SelectContent, { children: allOptions
                                        .filter((o) => o > (value[0] ?? min))
                                        .map((o) => (_jsx(SelectItem, { value: String(o), children: String(o) }, o))) })] })] }) })) : (_jsx("div", { className: "openui-slider-block__controls is-single", children: _jsxs(Select, { value: String(value[0]), disabled: disabled, onValueChange: (val) => setValueAndCommit([Number(val)]), children: [_jsx(SelectTrigger, { children: _jsx(SelectValue, {}) }), _jsx(SelectContent, { children: allOptions.map((o) => (_jsx(SelectItem, { value: String(o), children: String(o) }, o))) })] }) }));
        }
        else {
            return isRange ? (_jsxs("div", { className: "openui-slider-block__controls openui-slider-block__controls--inputs is-range", children: [_jsxs("div", { className: "openui-slider-block__validated-input-container", children: [_jsx(ValueInput, { value: value[0] ?? min, onChange: (newMin) => setValueAndCommit([newMin, value[1] ?? max]), error: minError, disabled: disabled }), _jsx("div", { className: "openui-slider-block__separator" }), _jsx(ValueInput, { value: value[1] ?? max, onChange: (newMax) => setValueAndCommit([value[0] ?? min, newMax]), error: maxError, disabled: disabled })] }), (minError || maxError) && (_jsxs("div", { className: "openui-slider-block__error-message", children: [_jsx(AlertCircle, { size: 14 }), " ", minError || maxError] }))] })) : (_jsxs("div", { className: "openui-slider-block__controls openui-slider-block__controls--inputs is-single", children: [_jsx(ValueInput, { value: value[0] ?? min, onChange: (newVal) => setValueAndCommit([newVal]), error: minError, disabled: disabled }), minError && (_jsxs("div", { className: "openui-slider-block__error-message", children: [_jsx(AlertCircle, { size: 14 }), minError] }))] }));
        }
    }, [
        isDiscrete,
        isRange,
        value,
        min,
        max,
        effectiveStep,
        minError,
        maxError,
        disabled,
        setValueAndCommit,
    ]);
    const hasError = !isDiscrete && (isRange ? Boolean(minError || maxError) : Boolean(minError));
    return (_jsxs("div", { className: "openui-slider-block", children: [_jsxs("div", { className: clsx("openui-slider-block__header", {
                    "openui-slider-block__header--with-error": hasError,
                }), children: [_jsx("span", { className: "openui-slider-block__label", children: label }), controlElements] }), _jsx("div", { className: "openui-slider-block__content", children: _jsx(Slider, { ...sliderProps, value: value, onValueChange: (v) => {
                        setValueAndCommit([...v].sort((a, b) => a - b));
                    }, min: min, max: max, step: effectiveStep, variant: variant, name: name, disabled: disabled }) })] }));
};
//# sourceMappingURL=SliderBlock.js.map