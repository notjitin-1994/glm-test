import { jsx as _jsx } from "react/jsx-runtime";
import { useLayoutEffect } from "react";
import { numberTickFormatter } from "../../utils";
const YAxisTick = (props) => {
    const { x, y, payload, textAnchor, verticalAnchor, className, setLabelWidth } = props;
    const displayValue = typeof payload?.value === "number"
        ? numberTickFormatter(payload?.value)
        : String(payload?.value);
    useLayoutEffect(() => {
        setLabelWidth(displayValue);
    }, [displayValue, setLabelWidth]);
    return (_jsx("g", { transform: `translate(${x},${y})`, className: className, children: _jsx("text", { x: 0, y: 0, dy: verticalAnchor === "middle" ? 4 : 0, textAnchor: textAnchor || "end", className: "openui-chart-y-axis-tick", children: displayValue }) }));
};
export { YAxisTick };
//# sourceMappingURL=YAxisTick.js.map