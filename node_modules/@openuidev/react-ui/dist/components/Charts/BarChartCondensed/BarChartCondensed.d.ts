import React from "react";
import { BarChartData, BarChartVariant } from "../BarChart/types";
import { SVGXAxisTickVariant } from "../shared";
import { PaletteName } from "../utils/PalletUtils";
export interface BarChartCondensedProps<T extends BarChartData> {
    data: T;
    categoryKey: keyof T[number];
    theme?: PaletteName;
    customPalette?: string[];
    variant?: BarChartVariant;
    tickVariant?: SVGXAxisTickVariant;
    grid?: boolean;
    radius?: number;
    icons?: Partial<Record<keyof T[number], React.ComponentType>>;
    isAnimationActive?: boolean;
    showYAxis?: boolean;
    xAxisLabel?: React.ReactNode;
    yAxisLabel?: React.ReactNode;
    legend?: boolean;
    className?: string;
    height?: number;
    width?: number;
    /** Maximum bar width in pixels. Prevents bars from becoming too wide. Default: 12 */
    maxBarWidth?: number;
}
declare const BarChartCondensedComponent: <T extends BarChartData>({ data, categoryKey, theme, customPalette, variant, tickVariant, grid, icons, radius, isAnimationActive, showYAxis, xAxisLabel, yAxisLabel, legend, className, height, width, maxBarWidth, }: BarChartCondensedProps<T>) => import("react/jsx-runtime").JSX.Element;
export declare const BarChartCondensed: typeof BarChartCondensedComponent;
export {};
//# sourceMappingURL=BarChartCondensed.d.ts.map