import React from "react";
import { XAxisTickVariant } from "../types";
import { PaletteName } from "../utils/PalletUtils";
import { AreaChartData, AreaChartVariant } from "./types";
export interface AreaChartProps<T extends AreaChartData> {
    data: T;
    categoryKey: keyof T[number];
    theme?: PaletteName;
    customPalette?: string[];
    variant?: AreaChartVariant;
    tickVariant?: XAxisTickVariant;
    grid?: boolean;
    legend?: boolean;
    icons?: Partial<Record<keyof T[number], React.ComponentType>>;
    isAnimationActive?: boolean;
    showYAxis?: boolean;
    xAxisLabel?: React.ReactNode;
    yAxisLabel?: React.ReactNode;
    className?: string;
    height?: number;
    width?: number;
}
declare const AreaChartComponent: <T extends AreaChartData>({ data, categoryKey, theme, customPalette, variant: areaChartVariant, tickVariant, grid, icons, isAnimationActive, showYAxis, xAxisLabel, yAxisLabel, legend, className, height, width, }: AreaChartProps<T>) => import("react/jsx-runtime").JSX.Element;
export declare const AreaChart: typeof AreaChartComponent;
export {};
//# sourceMappingURL=AreaChart.d.ts.map