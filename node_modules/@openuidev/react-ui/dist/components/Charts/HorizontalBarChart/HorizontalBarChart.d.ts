import React from "react";
import { type PaletteName } from "../utils/PalletUtils";
import { HorizontalBarChartData, HorizontalBarChartVariant } from "./types";
export interface HorizontalBarChartProps<T extends HorizontalBarChartData> {
    data: T;
    categoryKey: keyof T[number];
    theme?: PaletteName;
    customPalette?: string[];
    variant?: HorizontalBarChartVariant;
    grid?: boolean;
    radius?: number;
    icons?: Partial<Record<keyof T[number], React.ComponentType>>;
    isAnimationActive?: boolean;
    showXAxis?: boolean;
    xAxisLabel?: React.ReactNode;
    yAxisLabel?: React.ReactNode;
    legend?: boolean;
    className?: string;
    height?: number;
    width?: number;
}
declare const HorizontalBarChartComponent: <T extends HorizontalBarChartData>({ data, categoryKey, theme, customPalette, variant, grid, icons, radius, isAnimationActive, showXAxis, xAxisLabel, yAxisLabel, legend, className, height, width, }: HorizontalBarChartProps<T>) => import("react/jsx-runtime").JSX.Element;
export declare const HorizontalBarChart: typeof HorizontalBarChartComponent;
export {};
//# sourceMappingURL=HorizontalBarChart.d.ts.map