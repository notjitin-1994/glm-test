import { jsx as _jsx } from "react/jsx-runtime";
import { useState } from "react";
const ScatterDot = ({ cx, cy, fill, variant = "circle" }) => {
    const [active, setActive] = useState(false);
    if (typeof cx !== "number" || typeof cy !== "number") {
        return null;
    }
    const OUTLINE_COLOR = "var(--openui-highlight)";
    const OUTLINE_WIDTH = 2;
    const displayRadius = active ? 5 : 3;
    if (variant === "square") {
        const sideLength = displayRadius * 2;
        return (_jsx("rect", { x: cx - displayRadius, y: cy - displayRadius, width: sideLength, height: sideLength, fill: fill, stroke: active ? OUTLINE_COLOR : "none", strokeWidth: OUTLINE_WIDTH, strokeLinejoin: "round", vectorEffect: "non-scaling-stroke", rx: 2, onPointerEnter: () => {
                setActive(true);
            }, onPointerLeave: () => {
                setActive(false);
            } }));
    }
    return (_jsx("circle", { cx: cx, cy: cy, r: displayRadius, fill: fill, stroke: active ? OUTLINE_COLOR : "none", strokeWidth: OUTLINE_WIDTH, vectorEffect: "non-scaling-stroke", onPointerEnter: () => {
            setActive(true);
        }, onPointerLeave: () => {
            setActive(false);
        } }));
};
export default ScatterDot;
//# sourceMappingURL=ScatterDot.js.map