import React from "react";
import { XAxisTickVariant } from "../types";
import { type PaletteName } from "../utils/PalletUtils";
import { BarChartData, BarChartVariant } from "./types";
export interface BarChartProps<T extends BarChartData> {
    data: T;
    categoryKey: keyof T[number];
    theme?: PaletteName;
    customPalette?: string[];
    variant?: BarChartVariant;
    tickVariant?: XAxisTickVariant;
    grid?: boolean;
    radius?: number;
    icons?: Partial<Record<keyof T[number], React.ComponentType>>;
    isAnimationActive?: boolean;
    showYAxis?: boolean;
    xAxisLabel?: React.ReactNode;
    yAxisLabel?: React.ReactNode;
    legend?: boolean;
    className?: string;
    height?: number;
    width?: number;
}
declare const BarChartComponent: <T extends BarChartData>({ data, categoryKey, theme, customPalette, variant, tickVariant, grid, icons, radius, isAnimationActive, showYAxis, xAxisLabel, yAxisLabel, legend, className, height, width, }: BarChartProps<T>) => import("react/jsx-runtime").JSX.Element;
export declare const BarChart: typeof BarChartComponent;
export {};
//# sourceMappingURL=BarChart.d.ts.map