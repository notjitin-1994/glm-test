import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useThread } from "@openuidev/react-headless";
import clsx from "clsx";
import { isChatEmpty } from "../_shared/utils";
/**
 * Type guard to check if image is a URL object
 */
const isImageUrl = (image) => {
    return typeof image === "object" && image !== null && "url" in image;
};
export const WelcomeScreen = (props) => {
    const { className } = props;
    const messages = useThread((s) => s.messages);
    const isLoadingMessages = useThread((s) => s.isLoadingMessages);
    // Only show when there are no messages
    if (!isChatEmpty({ isLoadingMessages, messages })) {
        return null;
    }
    // Check if children are provided
    if ("children" in props && props.children) {
        return (_jsx("div", { className: clsx("openui-copilot-shell-welcome-screen", className), children: props.children }));
    }
    // Props-based content
    const { title, description, image } = props;
    const renderImage = () => {
        if (!image)
            return null;
        if (isImageUrl(image)) {
            return (_jsx("img", { src: image.url, alt: title || "", className: "openui-copilot-shell-welcome-screen__image" }));
        }
        return image;
    };
    return (_jsxs("div", { className: clsx("openui-copilot-shell-welcome-screen", className), children: [image && (_jsx("div", { className: "openui-copilot-shell-welcome-screen__image-container", children: renderImage() })), (title || description) && (_jsxs("div", { className: "openui-copilot-shell-welcome-screen__content", children: [title && _jsx("h2", { className: "openui-copilot-shell-welcome-screen__title", children: title }), description && (_jsx("p", { className: "openui-copilot-shell-welcome-screen__description", children: description }))] }))] }));
};
export default WelcomeScreen;
//# sourceMappingURL=WelcomeScreen.js.map